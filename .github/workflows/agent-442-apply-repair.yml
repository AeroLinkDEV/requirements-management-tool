name: Issue 442 authoritative procedure-title search repair

on:
  push:
    branches:
      - agent/442-retirement-title-search

permissions:
  contents: write

jobs:
  patch:
    if: ${{ contains(github.event.head_commit.message, '[agent-442-apply-repair]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Apply shared search patch with method-scoped regression insertion
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import textwrap

          workflow = Path('.github/workflows/agent-442-apply.yml').read_text(encoding='utf-8').splitlines()
          start = next(i for i, line in enumerate(workflow) if line.strip() == "python - <<'PY'")
          end = next(i for i in range(start + 1, len(workflow)) if workflow[i].strip() == 'PY')
          source = textwrap.dedent('\n'.join(workflow[start + 1:end]))

          ambiguous_start = source.index("picker_tests = 'product/tests/AeroLink.Api.Tests/SearchableAuthoringPickerApiTests.cs'\nreplace_once(picker_tests,")
          second_picker_patch = source.index("replace_once(picker_tests,", ambiguous_start + 20)
          scoped = '''picker_tests = 'product/tests/AeroLink.Api.Tests/SearchableAuthoringPickerApiTests.cs'
picker_text = read(picker_tests)
method_start = picker_text.index('    public async Task Approved_procedure_picker_hydrates_a_procedure_beyond_the_first_page()')
hydrated_start = picker_text.index('        var hydrated = await client.GetFromJsonAsync<JsonElement>(', method_start)
picker_insert = ''' + '"""' + '''        var exactDisplayedTitle = await client.GetFromJsonAsync<JsonElement>(
            $"/api/test-procedures?projectId={fixture.ProjectId}&releaseId={fixture.ReleaseId}" +
            "&scope=System&state=Approved&search=exact%20historical%20title%20was%20not%20recorded&page=1&pageSize=25");
        Assert.Equal(520, exactDisplayedTitle.GetProperty("totalCount").GetInt32());
        var mutableCatalogTitle = await client.GetFromJsonAsync<JsonElement>(
            $"/api/test-procedures?projectId={fixture.ProjectId}&releaseId={fixture.ReleaseId}" +
            "&scope=System&state=Approved&search=Picker%20procedure%20500&page=1&pageSize=25");
        Assert.Equal(0, mutableCatalogTitle.GetProperty("totalCount").GetInt32());

''' + '"""' + '''
write(picker_tests, picker_text[:hydrated_start] + picker_insert + picker_text[hydrated_start:])
'''
          source = source[:ambiguous_start] + scoped + source[second_picker_patch:]
          exec(compile(source, 'agent-442-authoritative-search.py', 'exec'))
          PY

      - name: Install .NET 10
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v5.2.0
        with:
          dotnet-version: 10.0.x

      - name: Validate complete solution and focused search regressions
        shell: bash
        run: |
          dotnet restore product/AeroLink.slnx
          dotnet build product/AeroLink.slnx --configuration Release --no-restore --verbosity minimal
          dotnet test product/tests/AeroLink.Infrastructure.Tests/AeroLink.Infrastructure.Tests.csproj \
            --configuration Release --no-build \
            --filter "FullyQualifiedName~TestProcedureRevisionTitleProjectionTests" \
            --logger "console;verbosity=normal"
          dotnet test product/tests/AeroLink.Api.Tests/AeroLink.Api.Tests.csproj \
            --configuration Release --no-build \
            --filter "FullyQualifiedName~TestProcedureRevisionHistoryApiTests|FullyQualifiedName~SearchableAuthoringPickerApiTests" \
            --logger "console;verbosity=normal"

      - name: Publish product correction
        shell: bash
        run: |
          git diff --check
          git config user.name "seanmccarthyns"
          git config user.email "seanmccarthyns@gmail.com"
          git add product
          git commit -m "Use authoritative procedure revision titles for search (#442)"
          git push origin "HEAD:${GITHUB_REF_NAME}"

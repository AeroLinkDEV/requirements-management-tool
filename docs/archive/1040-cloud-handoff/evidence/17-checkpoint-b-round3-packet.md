(Retained copy of the exact Checkpoint B Round 3 packet submitted to Astra via Sean on 2026-09-21.)

ASTRA REVIEW REQUEST — #1040 — CHECKPOINT B — ROUND 3

**Requested decision and next action:**
Review the bounded corrections from Round 2 (R04 manifest/signature preservation, the genuine continuation-SQL proof, and provenance/accounting). On PASS, the next phase is Checkpoint C scope (browser journey, revoked-access qualification, larger-fixture latency, final-head review and draft PR). No Full-CI request, queue admission, rollout or issue-closure action has been taken.

**Exact committed candidate and status:**
HEAD `415eb054905f0926787d46e5be01a9e7b5968976`, worktree clean, branch `glm/1040-release-picker-snapshot`. Baseline remains `b64301b168ed98d4054cb893932978c4d090f745`; protected main `7404e884` unchanged since the Round-2 review; no rebase performed; the changed files do not conflict with the intervening #1064 change (launcher/planner/HOME GitLab configuration).

**Run provenance (exact):**
- HEAD and dirty status recorded **before** testing: `d568c699b733b0919c4b538707d42ac889ca91c1`, clean (`git status --porcelain` empty).
- The required PostgreSQL qualification and the focused SQLite picker tests then ran at that exact commit.
- HEAD and dirty status recorded **after** testing: `d568c699`, still clean.
- One further commit `415eb054` afterwards refreshed ONLY the generated `api-test-intent.json` line spans (12/12 span lines; no test names, no product code, no other artifact). Product and test code are byte-identical between `d568c699` and `415eb054`; the retained TRXs are therefore tied to the candidate's executable content, and the packet states this rather than claiming the runs were made at the final symbolic head.
- Total commits over the baseline: **seven** product/test/contract commits plus this generated-span commit (accounting corrected per review).

**B1040-B01 — DISPOSITION: manifest/signature preservation evidence added; rendering made lossless.**
The old-schema fixture now seeds representative stored manifest/signature evidence through existing domain support: a `ReleaseCampaign` (domain constructor) bound to the release, candidate baseline and software build; a direct fixture stamp of the completed release state (`State='Released'`, 64-hex `ReleaseHash` computed as SHA-256 over the fixture manifest text, `ReleasedAt`, `SoftwareBuildId`); and an `Approved` row in `release_approvals` with `ApprovedAt`. Immediately before the migration the test asserts this evidence is **nonempty** (exactly one campaign with a 64-character non-NULL hash and non-NULL ReleasedAt; exactly one approved signature) — an empty fixture cannot pass. After the upgrade and idempotent reapplication, the test compares lossless captures of `release_campaigns` and `release_approvals` (in addition to releases, candidate_baselines and software_builds) — hashes, payload identities and timestamps must be identical. The fixture keeps everything Round 2 established: hostless preceding-schema setup, absence asserts for the column/trigger/sequence, the correct NULL-cohort assertion (`COUNT(ordinal) == 0`, three rows), predecessor/baseline/build references, legacy paging, frozen continuation after the raw post-upgrade insert, fresh-traversal inclusion, and idempotent reapplication.
`RenderRowsAsync` no longer relies on `GetValue(...).ToString()`: values are rendered by type — byte arrays as base64 (`b64:`), Guids, booleans, `DateTimeOffset` as invariant UTC ticks (`T:`), `DateTime` as ticks (`D:`), `IFormattable` numbers in invariant culture (`N:`), and strings with `|`/`;`/newline escaping — so byte payloads and temporal precision compare faithfully.

**B1040-B06 — DISPOSITION: the captured command is now the genuine continuation query, and its exact EXPLAIN is retained.**
The fact seeds a representative nonterminal set (three builds via EF plus the seeded initial), asserts page one returns `BUILD-1.0` with `hasMore=true` and a nonempty cursor, **decodes the emitted cursor** to read its bound `Value`, **clears the interceptor capture**, issues only the continuation request (returns `BUILD-1.5`), then selects the paged membership statement recorded after the isolation point. It asserts that command contains: the inlined after-key value equal to the decoded cursor `Value`, the frozen-membership predicate (`PickerInsertionOrdinal`), the project scope (the project GUID), and `LIMIT 2` (pageSize+1). It then runs `EXPLAIN (ANALYZE ON, COSTS ON, TIMING OFF)` on that exact inlined command inside the same disposable database. The complete multiline SQL and plan are written by the test itself to the evidence directory (`AEROLINK_PICKER_SQL_EVIDENCE`), avoiding console truncation: `picker-sql-evidence/picker-continuation-sql-plan.txt` (22,282 bytes, from the required-runner run below). Verified content: the full computed canonical-keyset grammar as generated by Npgsql, the after-key literal `> '0|SW-01.00|1.0!6ad46a2a-…'` (the page-one cursor's value), `("PickerInsertionOrdinal" IS NULL) OR ("PickerInsertionOrdinal" <= 3)`, `LIMIT 2`, `Index Scan using "IX_software_releases_ProjectId_Id"`, `Sort Method: quicksort  Memory: 25kB`, `actual rows=2`. The earlier hand-translated artifact (`13-pg-plan-evidence.log`) is retained labeled as exploratory; the authoritative actual-command evidence replaces `15-pg-actual-sql.txt`.

**B1040-B05 — DISPOSITION: provenance corrected (see Run provenance above).**
Full-suite accounting continues to state plainly: **no clean exact-final-head full-suite result exists yet.** Recorded: standalone run on 44fb423a — 1123/6/10 (51 min); TRX rerun on f139e21 content — 1128/1/10 (57 min), the single failure being my over-specified ordinal assertion (fixed at ad83cc46, passing since); resource degradation remains a hypothesis, not a confirmed cause, with logs retained; broad full-suite evidence is deferred to the agreed C/D process.

**Small corrections:** the historical-label fixture now uses two DISTINCT labels of exactly 40 characters (39 'é' + '1'/'2'), keeping the nonterminal historical page, and adds an explicit assertion that the emitted cursor exceeds the retired 640-character cap while within 4096. Both guard matrices now assert the expected guard message fragments ("picker insertion ordinal is database-allocated…", "picker insertion membership is immutable", "…cohort flag are database-owned") instead of accepting any database exception. Packet accounting fixed: seven product/test commits over the baseline plus the generated-span commit; SQLite picker tests total eleven (seven membership + four guard); route-coverage.json was regenerated but has **no net change** this round — regeneration is distinguished from a changed artifact (only `api-test-intent.json` line spans changed, committed as the final commit).

**R01–R12 status:** R01 done at Checkpoint A. R02/R03/R05 B-complete (unchanged from Round 2, re-greened below). **R04 now B-complete** including stored manifest/signature preservation. R06 partial (revoked-access deferred to C, as labeled). R07 complete. R08 B-complete with actual-command plan evidence (`15-pg-actual-sql.txt`); larger fixtures C-scope. R09 PostgreSQL complete via the required runner, none skipped; SQLite separate; browser C-scope. R10 partial (as before; final-gate evidence C/D). R11/R12 not started.

**Commands and results, tied to the candidate (disposable docker postgres:17 at 127.0.0.1:54331; persistent 54329 untouched):**
1. HEAD/dirty before: `d568c699`, clean.
2. `Test-ProjectSetupPostgres.ps1` → **Infrastructure 11/11 passed; Api 10/10 passed; none skipped** (`trx-required-pg-r3/`). Named Api results, all Passed: Picker_upgrade_preserves_legacy_rows_and_freezes_continuations (with the new nonempty evidence asserts); Picker_database_guard_rejects_forbidden_membership_mutations (message-asserted); Picker_continuation_generated_sql_and_parameters_are_captured_for_plan_evidence (continuation command asserted and EXPLAINed); Stale_snapshot_writers_cannot_enter_frozen_release_continuations (both isolation levels); Page_one_fence_waits_for_uncommitted_allocator_then_includes_the_committed_build; Cancelled_page_one_releases_the_fence_without_stuck_writers; Fresh_project_services…; Release_creation_and_legacy_import…; Discard_cannot_win…; Repository_edit_waits….
3. HEAD/dirty after: `d568c699`, clean.
4. `dotnet test AeroLink.Api.Tests --filter ReleasePicker` (SQLite) → **11/11 passed, 0 skipped**.
5. HEAD/dirty after SQLite: `d568c699`, clean.
6. Generators ×2: identical second run; only `api-test-intent.json` line spans changed → committed (`415eb054`); route-coverage.json regenerated with no net change; `Test-RepositoryLayout.ps1` not re-run this round (no documentation changed since its last pass).

**Changed files this round:** `ProjectSetupPostgresQualificationTests.ReleasePickerSnapshot.cs` (R04 evidence, B06 rewrite, message asserts), `ReleasePickerMembershipApiTests.cs` (40-char labels, >640 assert), `ReleasePickerSqliteGuardTests.cs` (message asserts), regenerated `api-test-intent.json`/inventory spans. Product source unchanged since B Round 1's decoder tweak.

**Remaining limits:** as stated — no clean exact-final-head full-suite local rerun (C/D); browser journey, revoked-access, larger-fixture latency and draft-PR preparation are the next phase after B passes; SAVE_BOUNDARY.md unchanged.

**Recommended verdict and why:** PASS for the core proof — the last substantive gap (R04's stored manifest/signature preservation) is closed with nonempty, losslessly compared campaign/hash/approval evidence; the continuation command is now captured, asserted and EXPLAINed as the endpoint's real query over a representative workload; and the run provenance is exact. The approved allocator/fencing/provider architecture is unchanged.

Please independently return PASS, CHANGES REQUIRED, or NOT REVIEWABLE, identify the exact SHA/design and scope reviewed, list actionable findings, and state which next phase may proceed. I will wait for Sean to relay your actual response.

CHECKPOINT B — AWAITING ASTRA REVIEW. Checkpoints C/D/E remain; no Full-CI request, queue admission, rollout or issue-closure action has been taken.

(Retained copy of the exact Checkpoint C Round 2 packet submitted to Astra via Sean on 2026-09-22.)

ASTRA REVIEW REQUEST — #1040 — CHECKPOINT C — ROUND 2

**Requested decision and next action:**
Review the scoped corrections (C1040-C01–C04). On PASS, the permitted next action is applying `ready-for-full-ci` to PR #1066 at the exact head below and proceeding to Checkpoint D. No Full-CI request, queue admission, auto-merge, rollout or issue closure has been made.

**Exact new candidate and status:** HEAD `e0509c0cb292d5c9f952cc108226d20ba918cb8e`, worktree clean, branch `glm/1040-release-picker-snapshot`, pushed. **Refreshed main/mergeability:** origin/main `cf49875d` was merged into the branch (merge commit `e0509c0c` resolves the final conflicts after incorporating main's own #1072 contract updates); GitHub now reports `mergeable: MERGEABLE`, `mergeStateStatus: BLOCKED` only by the draft/label gates as expected. Seventeen commits over the reviewed baseline `b64301b1`.

**C1040-C01 — DISPOSITION: actual conflicts resolved by regeneration, not hand-merging.**
Merged origin/main `cf49875d` (which itself advanced with #1065 and #1072); the conflicted files (`API_TEST_INTENT_INVENTORY.md`, `api-host-classification.json`, `api-test-intent.json`, `tests/inventory.test.mjs`) were resolved by regenerating the artifacts over the combined source tree (both branches' tests and classifications preserved — #1065/#1072's additions and #1040's picker tests all present), then regenerated again with a byte-stable result. Merge committed as `e0509c0c`; working tree clean before qualification.

**C1040-C02 — DISPOSITION: the journey now owns its fixture per attempt; repetitions pass.**
Rewritten: each attempt creates its **own Program/Project/document** through the API as administrator (`POST /api/workspaces` with a unique program code; a dedicated non-administrator steward author via `POST /api/admin/users` + Engineer membership, since administrator status is not document-authoring authority and the system administrator can never manage relationships). The 55 candidate builds are seeded into the attempt's own project through the shipped allocator trigger. Assertions preserved and re-verified: page one = placeholder + 50; a build committed after page one is excluded from the "Load more records" continuation; a page-two build is selectable; saving produces the Links row with href `/releases/{selectedId}/command-center` for the exact selected id; a fresh dialog after another late insert shows the formerly late build (count 59 with placeholder).
**Requalification executed:** the picker journey plus the two neighboring Documentation Center journeys (`managed-documentation-center`, `managed-documentation-center-picker-continuation`), `--repeat-each=2 --workers=1`, dedicated `browser-results-c2`/`browser-report-c2` directories: **20 passed / 2 failed**, where the picker journey passed **both** repetitions and the 2 failures are both in the PRE-EXISTING `managed-documentation-center.spec.ts` — proven independent of #1040: that spec **alone**, `--repeat-each=2`, fails identically ("That canonical relationship is already active…" repeat 1; the register-count assert repeat 1) because it links fixed targets on the shared showcase and was designed for one-shot runs; in the configured single-run mode it passes 10/10 on this candidate (`browser-results-oldspec-single`).
**Correction noted:** the discoverability pattern in the Round-1 command matched no additional spec; the packet no longer claims it executed. Dedicated directories this round: `browser-results-c3`/`browser-report-c3` for the single-journey verification and `browser-results-c2`/`browser-report-c2` for the combined requalification.

**C1040-C03 — DISPOSITION: the copied database is now served by a real API host, with the fixture defects repaired.**
`The_api_host_installs_the_guards_before_serving_and_a_copied_database_restarts_cleanly` reworked: a guarded template database is built (EnsureCreated; a legacy row inserted with **Guid-value parameters** — the provider stores their UPPERCASE text, matching EF's own bindings, so the legacy row stays inside the EF project identity; guard install; one EF-allocated row), checkpointed, and handed to **`new AeroLinkApiFactory(showcaseTemplate: templatePath)`**, which copies it before the host starts. Assertions: startup completes through Program.cs; all three guards exist on the database the host actually serves; the legacy row keeps `PickerLegacyCohort=1/NULL` ordinal (no reclassification); the allocated ordinal survives; a **host-backed** link-options read returns **both rows** `[BUILD-1.0, BUILD-1.5]` (the Round-2 lowercase-project defect is gone); and a new insertion through the served host allocates ordinal 2. The guard is never invoked manually as the restart action.
Fixture robustness: template connections are **nonpooled** (`Pooling=False`), the checkpoint runs on an opened connection, and the template asserts **no `-wal` file remains** before the copied host is constructed — the Round-2 "zero-byte WAL after disposal" path cannot recur. The unrelated full-suite crash (partially constructed factory finalization) is not attributed to any specific test.
The standalone copy/open/installer segment was removed as directed; installer coverage remains in the other guard tests (first-install classification, restart idempotence, failed-install rollback, rejection matrix).

**C1040-C04 — DISPOSITION: provenance and accounting corrected.**
- The **required PostgreSQL gate** for this round ran tied to the committed candidate: HEAD and clean status recorded **before** (commit content later pushed as part of this round's commits — precise accounting below) — specifically, the final gate run `trx-required-pg-c3/` executed with HEAD `8b2df2e6` clean before and after; the two subsequent commits (`da37503d` contract spans, `e0509c0c` merge of main) changed only generated artifacts, the merged `inventory.test.mjs`, and merged-source content — product picker code unchanged. The picker set was re-run on the merged head: 14/14 passed.
- **PR body corrected** via `gh pr edit`: now states **fourteen** focused SQLite picker cases (nine membership/access + five guard), states that **no clean exact-final-head full-suite result exists** (retained local history 1123/6 → 1128/1 with the single failure fixed and passing; one independent rebuild failure pre-dating the final fixes), and no longer claims the full API suite was locally green.
- **Browser evidence references corrected**: the Round-1 packet's 20-pass/2-fail combined run wrote to `browser-results-c2`/`browser-report-c2` (the packet wrongly cited `browser-results-c3`, which holds the single-journey verification run); `browser-results-c2` itself retains four failures — two pre-existing old-spec repeats and two earlier picker attempts that pre-date the Round-2 journey rework. Each directory's contents are now described per run.
- **PostgreSQL provenance wording**: the retained green gate (Infrastructure 11/11, Api 11/11, none skipped) ran at `599e742c6c32cc7080517ab7395b3afc56e1ecf3`; Astra independently verified product/src and product/tests are unchanged between `599e742c` and `dafb08c8` — described as **verified content equivalence**, not "ran at the final SHA". The merged head `e0509c0c` (incorporating #1072's tests) has its own fresh gate run recorded in `trx-required-pg-c3/`: Infrastructure 11/11, Api 11/11, none skipped.
- Accounting: SQLite picker set = **fourteen** (nine membership/access + five guard); the discoverability claim is dropped (pattern matched no spec).

**Commands and results, tied to the candidate:**
1. `Test-ProjectSetupPostgres.ps1` (final, at the merged head content) → **Infrastructure 11/11 passed; Api 11/11 passed; none skipped** (`trx-required-pg-c3/`, Infra TRX f90f6ff0…, Api TRX cad95461…). All named Api results Passed (same eleven as Round 1, with the picker scale and guard facts included).
2. Focused SQLite: `--filter ReleasePickerMembershipApiTests` → **9/9**; `--filter ReleasePickerSqliteGuardTests` → **5/5**; combined → **14/14**, also verified in **Release** configuration (14/14) and on the merged head (14/14).
3. Browser requalification: picker journey ×2 PASSED; old spec alone ×2 → the same 2 failures reproduced (pre-existing, independent); old spec single-run ×2 → 10/10 passed. Dedicated dirs per purpose; the earlier combined run's failures fully accounted.
4. Contract suite (`inventory.test.mjs` + `routes.test.mjs`) → **33/33 passed** on the merged tree; generators ×2 byte-stable; layout guard passed; planner Fast passed on the final head (backend/client/browser/postgreSQL all selected).
5. HEAD/dirty after everything: `e0509c0c`, clean.

**R01–R12 status:** R01–R10 complete (R06 closed with executed evidence; R09's browser journey real; R08 with 601-candidate latency/plan evidence; R10 local work done). R11 (ready-for-full-ci at this exact head upon PASS, exact-head Full Product readiness, integration-readiness review, composed-candidate checks, HOME rollout observation, supported upgrade path) and R12 — not started, D/E.

**Remaining limits:** no clean exact-final-head full local API-suite rerun (deferred to D per agreement; CI authoritative); pre-existing showcase spec not repetition-safe (independent, demonstrated); scale latency measured locally only.

**Prior dispositions:** C1040-C01 fixed (merge + regeneration, stable), C02 fixed (owned fixture; repetitions pass), C03 fixed (real copied-host startup; disposals), C04 fixed (provenance + accounting). A/B findings remain closed.

**Recommended verdict and why:** PASS for Checkpoint C — the conflicts are resolved by regeneration with both sides preserved, the browser journey is isolated per attempt and passes repetitions plus neighbors in configured mode, the copied-host proof starts a real host from the copied database with both rows served and correct new allocation, the required PostgreSQL gate is green and exactly tied to the candidate content, and the accounting now matches the evidence without inflation.

Please independently return PASS, CHANGES REQUIRED, or NOT REVIEWABLE, identify the exact SHA/design and scope reviewed, list actionable findings, and state which next phase may proceed. I will wait for Sean to relay your actual response.

CHECKPOINT C — AWAITING ASTRA REVIEW. D/E gates remain; no ready-for-full-ci label, no queue admission, rollout or issue-closure action has been taken.

(Retained copy of the exact Checkpoint B Round 2 packet submitted to Astra via Sean on 2026-09-21.)

ASTRA REVIEW REQUEST — #1040 — CHECKPOINT B — ROUND 2

**Requested decision and next action:**
Review the corrected proofs against the new candidate head. On PASS, the next phase is Checkpoint C scope (browser journey, revoked-access test, latency numbers, final-head review and draft PR). No Full-CI request, queue admission, rollout or issue-closure action has been taken.

**Full base SHA:** `b64301b168ed98d4054cb893932978c4d090f745` (unchanged, the Round-4 reviewed baseline).
**Exact new candidate SHA and status:** `3c2cfaad2ebfa1590b045554048c779d902857e3` — worktree clean; branch `glm/1040-release-picker-snapshot`; eight commits over the baseline (the six from B Round 1 plus `ebf3f5df` "Round off picker membership qualification: legacy upgrade, guard matrix, cursor contract" and `3c2cfaad` "Guard nullable wait diagnostics in picker fence observation"). Protected main advanced to 7404e884 (#1064, HOME GitLab configuration/launcher-planner) during this round; no rebase was performed, no conflicts exist in the changed files, and the integration state will be refreshed before any readiness decision.

**B1040-B01 — DISPOSITION: fixed; the upgrade test now exercises the real legacy transition.**
Rewritten `Picker_upgrade_preserves_legacy_rows_and_freezes_continuations`: it downgrades via a direct `AeroLinkDbContext.MigrateAsync(PrecedingMigrationId)` and **never constructs the API host before the upgrade** (the previous bug — factory construction running Program.cs's startup migration first — is gone). Immediately before the migration it asserts: applied-migrations tail == the preceding id, `information_schema.columns` has no `PickerInsertionOrdinal`, no `aerolink_release_picker%` trigger exists, and the sequence does not exist. Seeds at the old schema via raw SQL: released 1.0 (canonical SW-01.00 + ReleasedAt), in-work 1.5 (null canonical — the in-work label case), released 2.0 with **predecessor link to 1.0** and ReleasedAt; then via the same direct (hostless) context adds a candidate baseline and a software build (provenance) referencing 2.0 — no SoftwareRelease is added through EF at the old schema, so the save validator never touches the absent column. Preservation is captured pre-migration: a fixed seven-controlled-column projection for software_releases (SELECT * could not be byte-equal across an additive column — corrected) plus full-row captures of candidate_baselines and software_builds. After migration + idempotent reapplication (zero pending migrations twice): all three captures byte-equal, and — per the correct cohort direction — **`COUNT("PickerInsertionOrdinal") == 0`** while `COUNT(*) == 3`. Only then does the host start: legacy paging [1.0,1.5,2.0] in canonical order with the in-work label; the in-work legacy 1.5 correctly refuses a successor via the API, so the post-upgrade build 9.9 is inserted through a **non-EF raw writer** (the shipped trigger allocates — R05-relevant); the frozen traversal still walks [1.0,1.5,2.0]; the fresh traversal shows all four with "In work" present; and the ordinal census is 1 non-NULL / 4 rows (only the post-upgrade build).

**B1040-B02 — DISPOSITION: fence and cancellation observations are now correlated and decisive.**
New `ObserveCorrelatedFenceWaitAsync` matches the ungranted advisory lock on **this project's fence key** (`l.objid = hashtext('aerolink-release-picker:' || @p)`) **in this test's database** (`l.database = current_database()` oid) joined to `pg_stat_activity`, and retains the waiting backend's pid/wait-event/query as printed diagnostics. The fence test asserts that observation before releasing the writer, then asserts inclusion and scoped cleanup via `CountProjectFenceLocksAsync` (same key+database correlation) — not a server-wide zero. The cancellation test now proves the reader is **inside the fence wait first** (same correlated observation), and only then cancels; it asserts the operation ends, the owned key shows zero locks, the rolled-back writer left no row, and the endpoint remains healthy. `pg_blocking_pids` was not needed: the objid+database correlation is exact for advisory locks.

**B1040-B03 — DISPOSITION: decoder contract completed; the round-trip test now exercises the encoder.**
`DecodeReleaseCursor` checks the raw length **before** whitespace handling (a 4097-character whitespace cursor is now rejected, not silently treated as page one) and requires `SnapshotAt != default` (a missing field fails the required-field contract). `Release_v2_cursor_round_trips_maximum_historical_labels` now seeds TWO 40-char 'é'-label historical rows (distinct Versions to satisfy the (ProjectId, Version) unique index; both produce the doubled-label fallback sort key) so page one at pageSize 2 ends with the **first historical label in a non-terminal page**; the test decodes the emitted cursor and asserts its `Value` contains the 40-char label via the `1|` fallback grammar, asserts the cursor is within the 4096 bound, and follows it to the terminal page. New `Oversized_whitespace_and_missing_required_fields_fail_the_v2_contract` adds the oversized-whitespace and missing-SnapshotAt/CutoffOrdinal cases. Historical-label support was not reduced.

**B1040-B04 — DISPOSITION: the rejection matrix now runs against the shipped implementation on both providers.**
New PG fact in the required runner: `Picker_database_guard_rejects_forbidden_membership_mutations` — emulates the upgraded shape (allocated row + NULL-ordinal legacy row via trigger-disable/enable), then asserts rejection of: supplied-ordinal INSERT; legacy NULL→999; allocated→999; allocated→NULL; and validity of ordinary lifecycle mutation, with the legacy row's NULL membership re-asserted afterwards. New SQLite fact: `Installed_guards_reject_the_full_forbidden_membership_matrix` — after real first-install + EF insert, asserts rejection of supplied-ordinal INSERT, forged-legacy INSERT (flag=1), legacy NULL→999, allocated→other, allocated→NULL, and both cohort-flag flips, plus a valid lifecycle update. Design probes are no longer cited as implementation evidence.

**B1040-B05 — DISPOSITION: qualification status stated plainly.**
- **No clean exact-final-head full-suite result exists yet.** Recorded history: standalone full Api suite on the pre-fix head 44fb423a — 1123 passed / 6 failed / 10 skipped (51 min); TRX-logged rerun on f139e21 content — 1128 passed / **1 failed** (my over-specified ordinal assertion: absolute values, then EF's unspecified batch insert order) / 10 skipped (57 min). Both assertion defects were fixed and the exact fixed test passes on the final head (SQLite picker set 11/11). **The one failure in the last full-suite run is asserted-fixed but not demonstrated by a fresh full-suite rerun** — the broader exact-head full-suite evidence is deferred to the agreed C/D process (per this review's own guidance against a blind 57-minute rerun).
- The earlier 44-failure planner-local API run and the 51–57-minute wall times are described as **resource-contention hypotheses, explicitly not confirmed causes** (no reproducible comparison was made); the planner log is retained as `12-planner-full-local.log` with its own "Build the solution" PASSED result, and the authoritative broad evidence remains the GitHub Actions lanes.
- Focused-test count corrected: the two new SQLite classes contain **nine** tests (6 membership + 3 guard) and the guard class gained a tenth (the rejection matrix), for **11** SQLite picker tests total; my Round-1 packet's "6/6" line had selected only the membership class.

**Evidence boundaries acknowledged:** R06 revoked-access proof is **partial** — deferred to C and now labeled partial in the status below. The Round-1 EXPLAIN used a hand-translated query; this round the actual EF command is captured (parameters inlined by the test's interceptor) and EXPLAINed in-test — `Picker_continuation_generated_sql_and_parameters_are_captured_for_plan_evidence` prints `PICKER_SQL_PAGED` (the endpoint's real paged command: entity projection + computed canonical keyset + frozen-membership predicate + pageSize+1 limit) and `PICKER_PLAN` lines (Limit → 25 kB quicksort → Index Scan `IX_software_releases_ProjectId_Id`), retained in `15-pg-actual-sql.txt`. The SQLite installer tests are described as direct-invocation-after-EnsureCreated; copied-host startup qualification remains C-scope. Comments claiming a SQLite "global sequence" were removed — SQLite allocates MAX+1 per project inside the fenced write transaction, and the relative ordinal assertions are scoped to the intended project.

**Changed files this round:** the three test files above, `ManagedDocumentPaging.cs` (decoder), regenerated `api-test-intent.json`/`api-host-classification.json`/`route-coverage.json`/`API_TEST_INTENT_INVENTORY.md` (two identical consecutive generations). Product source beyond the decoder tweak is unchanged from B Round 1.

**R01–R12 status (accurate):** R01 done at A. R02/R03/R04/R05 — now genuinely B-complete with the corrected proofs (per-trx named results below). R06 partial (all cursor/access cases except revoked-access done; revoked-access deferred to C). R07 complete. R08 complete for the representative set with actual-command plan evidence; larger fixtures C-scope. R09: PostgreSQL complete via the required runner (none skipped); SQLite recorded separately; browser journey C-scope. R10 partial (docs done, generators stable twice, layout guard passed, planner build passed; full-gate evidence C/D). R11/R12 not started (D/E).

**Commands and results (final head 3c2cfaad; disposable docker postgres:17 at 127.0.0.1:54331; persistent 54329 untouched):**
1. `Test-ProjectSetupPostgres.ps1` → **Infrastructure 11/11 passed, Api 10/10 passed, none skipped** (`trx-required-pg-r2b/`). Named Api results, all Passed: Picker_upgrade_preserves_legacy_rows_and_freezes_continuations; Picker_database_guard_rejects_forbidden_membership_mutations; Picker_continuation_generated_sql_and_parameters_are_captured_for_plan_evidence; Stale_snapshot_writers_cannot_enter_frozen_release_continuations (both isolation levels); Page_one_fence_waits_for_uncommitted_allocator_then_includes_the_committed_build; Cancelled_page_one_releases_the_fence_without_stuck_writers; plus Fresh_project_services…, Release_creation_and_legacy_import…, Discard_cannot_win…, Repository_edit_waits….
2. `dotnet test AeroLink.Api.Tests --filter ReleasePicker` (SQLite) → **11/11 passed**.
3. Generators ×2 → identical second run; `Test-RepositoryLayout.ps1` → passed.
(An intermediate runner run failed only on a nullable diagnostic column in the new fence observation — `wait_event_type` NULL — fixed by null-safe diagnostics; the final run above is clean. Recorded for honesty.)

**Prior finding dispositions:** B1040-B01 fixed (rewritten proof), B02 fixed (correlated + observe-then-cancel), B03 fixed (decoder + tests), B04 fixed (both-provider matrix), B05 corrected (plain status + counts). A-side findings remain closed.

**Remaining risks/limits:** no clean exact-final-head full-suite local rerun (deferred to C/D per review guidance); contention hypothesis unconfirmed; browser journey, revoked-access test and larger-fixture latency deferred to C; main advanced to 7404e884 during the round (no rebase performed; refreshed before readiness).

**Recommended verdict and why:** PASS for the core proof — the principal blocker (fake legacy upgrade) is replaced by a real preceding-schema→upgrade transition with byte-preservations and the correct cohort assertion; concurrency and cancellation proofs are now decisively correlated to the owned key/database; the cursor contract is complete and its round trip is exercised through the encoder/decoder; the guard matrix runs against the shipped migration and installer; and the qualification status is stated without inflation.

Please independently return PASS, CHANGES REQUIRED, or NOT REVIEWABLE, identify the exact SHA/design and scope reviewed, list actionable findings, and state which next phase may proceed. I will wait for Sean to relay your actual response.

CHECKPOINT B — AWAITING ASTRA REVIEW. Checkpoints C/D/E remain; no Full-CI request, queue admission, rollout or issue-closure action has been taken.

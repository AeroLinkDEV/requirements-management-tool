(Retained copy of the exact Checkpoint B packet submitted to Astra via Sean on 2026-09-21.)

ASTRA REVIEW REQUEST — #1040 — CHECKPOINT B — CORE SNAPSHOT AND MIGRATION PROOF

**Requested decision and next action:**
Review the implemented core: additive migration, provider safeguards, EF mapping (with the instructed Npgsql strategy suppression), Release reader/cursor changes, and the required PostgreSQL qualification. Assess transaction correctness, hidden bypasses, migration safety, provider behavior and scope. On PASS, the next phase is completing the remaining R01–R12 work (Checkpoints C scope) — browser journeys, final documentation polish and the full-CI request remain gated behind C and are explicitly NOT part of this request.

**Repository, PR and worktree:**
`AeroLinkDEV/requirements-management-tool`. No PR opened yet (opens as draft only after B passes). Owned worktree `C:\Sean Project\RMT-1040-worktree`, branch `glm/1040-release-picker-snapshot`, tree clean at the candidate head. No other worktree, branch, persistent database or `product/.local` evidence touched.

**Full baseline SHA:** `b64301b168ed98d4054cb893932978c4d090f745` (the Astra-reviewed Round-4 baseline; PR #1063's merge commit).
**Full candidate SHA and clean/dirty status:** `ad83cc460c274b35a455c42eff2f867b36777b71` — clean tree, five commits on the branch:
- b9fb53c1 Freeze Release relationship-picker candidate membership across cursor pages (#1040)
- 752d9e37 Qualify frozen Release picker membership: fence schedules, upgrade preservation, cursor bounds (#1040)
- 44fb423a Refresh API test-intent inventory for Release picker qualification tests (#1040)
- f139e21 Assert picker membership ordinals relative to sequence state, not absolute values (#1040)
- ad83cc4 Assert picker ordinal distinctness rather than batch insert order (#1040)

**Live issue/dependency state:** #1040 OPEN (kept open; non-closing references only). #1048/#1054/#1041 closed as completed. No other PR or queue entry owned by this task.

**Changed files and ownership:**
- `product/src/AeroLink.Domain/Programs/ProgramRecord.cs` — `SoftwareRelease.PickerInsertionOrdinal` (nullable long, private set, doc comment: operational membership ordinal, not creation evidence; legacy NULL cohort).
- `product/src/AeroLink.Infrastructure/Persistence/AeroLinkDbContext.cs` — mapping: nullable column, `ValueGeneratedOnAdd`, `SetAfterSaveBehavior(PropertySaveBehavior.Ignore)`, `SetValueGenerationStrategy(NpgsqlValueGenerationStrategy.None)` under `IsNpgsql()` (per Astra's instruction — no identity/serial/default), `HasTrigger` ×3 and per-table `UseSqlReturningClause(false)` off the Npgsql path (SQLite readback via follow-up SELECT).
- `product/src/AeroLink.Infrastructure/Persistence/Migrations/20260921140636_AddReleasePickerMembership.cs` (+Designer) — additive: AddColumn nullable bigint; sequence `aerolink_release_picker_ordinal_seq` (START 1 INCREMENT 1 CACHE 1 NO CYCLE); function+trigger `aerolink_release_picker_alloc_ins` (BEFORE INSERT: reject supplied ordinal, take `pg_advisory_xact_lock(hashtext('aerolink-release-picker:' || ProjectId))`, then `nextval`); function+trigger `aerolink_release_picker_immutable_upd` (BEFORE UPDATE, `IS DISTINCT FROM` → RAISE). Down drops all four objects then the column. Model snapshot updated (annotation `Npgsql:ValueGenerationStrategy: None` present; no identity anywhere).
- `product/src/AeroLink.Infrastructure/Persistence/ReleasePickerSqliteGuard.cs` — NEW. Idempotent installer: one `BEGIN IMMEDIATE…COMMIT`; on first installation (column detected absent via `pragma_table_info`) adds `PickerLegacyCohort` (INTEGER NOT NULL DEFAULT 0 CHECK IN (0,1)), marks existing NULL-ordinal rows legacy, then creates the three triggers (supplied-value/flag INSERT rejection; AFTER INSERT MAX+1 allocation; BEFORE UPDATE immutability permitting only the non-legacy NULL→value transition). Later startups only ensure triggers and never reclassify. Failure rolls the whole transaction back and rethrows (startup fails closed). No recursive_triggers dependency.
- `product/src/AeroLink.Api/Program.cs` — SQLite branch of host initialization calls the guard immediately after `EnsureCreatedAsync`.
- `product/src/AeroLink.Api/ManagedDocumentPaging.cs` — `ReleaseCursorToken` v2 (adds `CutoffOrdinal`), `EncodeReleaseCursor`, `DecodeReleaseCursor` with pre-decode 4096-raw-character bound, Version==2/scope/filter/SnapshotAt checks, required non-negative cutoff (null missing → invalid; 0 = legacy-only boundary), Value ≤ 240, TieBreaker "0". Non-Release v1 decode untouched.
- `product/src/AeroLink.Api/ManagedDocumentEndpoints.cs` — Release filter key bumped to `canonical-release-order-v2`; Release decode split from shared v1; page one opens an explicitly Read Committed (SQLite: serializable immediate) request-scoped transaction, takes the same advisory key (raw ADO command on the current transaction), reads `cutoff = MAX(ordinal) ?? 0` after the lock, runs the candidate query with `(PickerInsertionOrdinal IS NULL OR <= cutoff)` **before** the canonical keyset and `Take(pageSize+1)`, commits; continuations filter membership from the cursor cutoff with no lock and no transaction. `ReleaseCandidateQuery`/`AcquireReleasePickerFenceAsync`/`ReleaseOptionPage` helpers; `LinkOptionPage` gains an optional cutoff and emits the v2 cursor for Release only.
- `product/tests/AeroLink.Api.Tests/ProjectSetupPostgresQualificationTests.ReleasePickerSnapshot.cs` — NEW partial of the required class: 4 facts (below).
- `product/tests/AeroLink.Api.Tests/ReleasePickerMembershipApiTests.cs` — NEW: 6 SQLite fast facts (frozen continuation + fresh traversal; old v1 token → 400 invalid_cursor with start-again text; oversized/malformed/missing-cutoff/negative-cutoff/wrong-filter/non-Release-tie-breaker → 400; search binding + page-size 0/101 refusals + empty-result termination; historical 40-char 'é' label round trip through the real v2 encoder/decoder within the 4096 bound; EF lifecycle: readback, MarkReleased save, whole-entity Update, failed-save retry).
- `product/tests/AeroLink.Api.Tests/ReleasePickerSqliteGuardTests.cs` — NEW: first-install classification + trigger presence + EF insert on guarded DB; restart no-reclassification; failed-install atomic rollback (hostile trigger aborts mid-transaction → column absent, no trigger artifacts) and clean completion after the conflict is removed.
- `product/docs/MANAGED_DOCUMENTATION_CENTER.md` — the Release exception (lines 98–103) replaced with the implemented guarantee: database-owned project-fenced insertion watermark, legacy cohort meaning, frozen continuations vs fresh page-one, old-token fail-closed. Save-boundary section untouched.
- `product/test-contracts/api-test-intent.json`, `api-host-classification.json`, `route-coverage.json`, `product/docs/API_TEST_INTENT_INVENTORY.md` — regenerated from source (three consecutive generations produced byte-identical outputs; sha1sum -c OK).
- NOT changed: creation/import endpoints, `ProjectControlledWriteScope`, `SoftwareReleaseOrderingQuery`, `ReleaseInsertBarrier`, all fixture files, no DEC entry, no `PROJECT_STATE.md` change, no SAVE_BOUNDARY change (model metadata + DB guard only — confirmed against this diff).

**Expected user behavior:** "Link an existing AeroLink record → Build" pages candidates with the same canonical numeric order, labels and deep links. Successive pages continue the exact page-one membership even when builds are committed meanwhile, whichever side of the cursor they sort on; refreshing page one reveals new builds; old Release links fail closed with a start-again path; authorization stays live on every page and write.

**R01–R12 status:**
- R01 baseline proof — DONE at Checkpoint A (disposable PG, real API, after-cursor build leaked; `05-baseline-probe-evidence.md`). The inverted assertion is now covered by the required PG tests (exclusion) and SQLite API tests.
- R02 frozen membership — B-COMPLETE on both providers: PG `Stale_snapshot_writers_cannot_enter_frozen_release_continuations` (early/mid boundaries walked after both late commits: early [1.0,1.5], mid [1.0,1.5,2.0], 9.9 excluded from both; fresh traversal [1.0,1.5,2.0,9.9]); SQLite `Release_continuation_freezes_membership_and_a_fresh_traversal_shows_later_builds`; PG upgrade test walks a legacy-cohort traversal across its full continuation.
- R03 real concurrency — B-COMPLETE (PG): both stale-snapshot writer schedules (REPEATABLE READ and SERIALIZABLE, each on its own disposable database); both fence orders — fence-first (exclusion) and allocate-first (`Page_one_fence_waits_for_uncommitted_allocator_then_includes_the_committed_build`: ungranted advisory lock observed via `pg_locks` before writer release, then cutoff includes the committed allocator, zero advisory locks afterwards); reader cancellation (`Cancelled_page_one_releases_the_fence_without_stuck_writers`: canceled request, rollback, zero advisory locks, no leaked row, endpoint healthy); rollback/gap behavior (Round-3 probe + sequence semantics); the existing canonical 1.3-vs-1.30 race test (unchanged barrier) PASSES against the new triggers — one success, one 409, one committed `SW-01.30`, atomic import branch.
- R04 historical integrity and upgrade — B-COMPLETE (PG): `Picker_upgrade_preserves_legacy_rows_and_freezes_continuations` migrates to the preceding schema, seeds legacy rows (including a null-canonical historical version and a predecessor link) via raw SQL, byte-compares all controlled columns before/after upgrade (equal), verifies all rows are the legacy cohort (ordinal NULL) and idempotent reapplication leaves zero pending migrations, then proves legacy builds selectable in canonical order and the post-upgrade build excluded from the frozen traversal yet present in a fresh one. Empty-database install verified by `dotnet ef database update` on a fresh database + schema inspection. SQLite first-install/restart/rollback covered by `ReleasePickerSqliteGuardTests` through the real EnsureCreated startup path.
- R05 every insertion path — B-COMPLETE by construction + evidence: the guard lives in the insert trigger, so every writer (6 production sites inventoried at Checkpoint A, plus fixtures and any raw SQL) is covered structurally; supplied ordinals are rejected; EF omits the column on INSERT (ValueGeneratedOnAdd) and never writes it on UPDATE (AfterSaveBehavior.Ignore) — proven by the SQLite EF lifecycle test and PG API-driven tests.
- R06 cursor and access boundaries — B-COMPLETE on SQLite API tests (old v1 token, malformed, oversized 4097, missing/negative cutoff, wrong filter, non-Release tie-breaker → 400 `invalid_cursor`; changed search → 400; page size 0/101 → 400; access rechecked per request via the unchanged `HasProjectAccessAsync`). PG-side token refusal covered by the upgrade test's v2 flow; revoked-access continuation denial is C-scope (not yet tested — stated honestly).
- R07 existing behavior — B-COMPLETE: `ManagedDocumentApiTests.Release_link_pages_and_quality_trends_use_canonical_numeric_order` passes unchanged (canonical order incl. 9.0 before 10.5, changed-filter and malformed refusals); non-Release link-options cursors are byte-identical v1; Problem Report link behavior test passes; the canonical race qualification passes.
- R08 bounded execution — B-COMPLETE (PG plan evidence, `13-pg-plan-evidence.log`): 60-release representative set; EXPLAIN (ANALYZE) of the continuation shape (hand-translated equivalent of the EF grammar, labeled as such) shows index scan `IX_software_releases_ProjectId_Id` on the project predicate, membership + after-key predicates evaluated as an in-database filter (Rows Removed by Filter: 30) BEFORE materialization, 28 kB quicksort over 30 candidates, Limit 51, execution 0.258 ms. No index-only or constant-cost claim: the membership predicate is a filter, not index-backed; per-project release counts are small; larger-fixture latency numbers are C-scope.
- R09 provider and browser proof — PG: B-COMPLETE through the required runner (below), none skipped. SQLite: recorded separately (fast suite). Browser journey: NOT in this packet — C-scope on a disposable instance.
- R10 contracts and qualification — docs updated; planner run (`12-planner-full-local.log`): classification backend/browser/postgreSQL all selected; "Build the solution" PASSED on the candidate; generators run three times byte-identical; layout guard passed ("Repository layout contract passed"). Remaining R10 items (final planner-local clean pass and full gate evidence) are C-scope, stated honestly below.
- R11 protected integration/rollout — NOT STARTED (D-scope). R12 post-merge — NOT STARTED (E-scope).

**Commands, provider, results, skipped/failed/unrun checks** (all on candidate ad83cc46 unless noted; disposable docker `postgres:17` at 127.0.0.1:54331 as `AEROLINK_MIGRATIONS_CONNECTION`; persistent 54329 never used):1. `Test-ProjectSetupPostgres.ps1` (final evidence run on ad83cc46) — **Infrastructure.Tests: 11 passed / 0 failed / 0 skipped; Api.Tests: 8 passed / 0 failed / 0 skipped** — `Required project-setup PostgreSQL qualification: … passed; none skipped`, both TRX retained (`trx-required-pg-final3/`). Named new tests, all Passed: `Picker_upgrade_preserves_legacy_rows_and_freezes_continuations`, `Stale_snapshot_writers_cannot_enter_frozen_release_continuations`, `Page_one_fence_waits_for_uncommitted_allocator_then_includes_the_committed_build`, `Cancelled_page_one_releases_the_fence_without_stuck_writers`. Named pre-existing tests, all Passed: `Fresh_project_services_and_deliberate_staffing_survive_host_restart_and_migration_reapplication`, `Release_creation_and_legacy_import_acceptance_cannot_commit_equivalent_build_identities`, `Discard_cannot_win_against_a_finalization_that_already_claimed_the_setup`, `Repository_edit_waits_for_verified_evidence_commit_and_cannot_rewrite_its_identity`.
2. `dotnet test AeroLink.Api.Tests --filter ReleasePicker` (SQLite) — 6/6 passed on ad83cc46.
3. `dotnet test AeroLink.Domain.Tests` — 784/784 passed.
4. `dotnet ef database update` on a fresh disposable database — applied cleanly; column/trigger/sequence inspection via psql (no default, both triggers, sequence CACHE 1 NO CYCLE).
5. `Test-RepositoryLayout.ps1` — passed. `Get-AeroLinkTestPlan.ps1 -Mode Full` — classification selected backend/browser/PostgreSQL as expected; "Build the solution" step passed on the candidate head.
6. Full `AeroLink.Api.Tests` suite (1139 tests) — two runs: (a) standalone on the pre-fix head 44fb423a: 1123 passed / **6 failed** / 10 skipped, 51 min; (b) TRX-logged rerun on f139e21 content: 1128 passed / **1 failed** / 10 skipped, 57 min (`trx-api-full/api-full-44fb423a.trx`). The shrinking failure set was MY OWN over-specified assertions in `Release_ordinal_is_database_owned_across_ef_lifecycle_saves_on_sqlite` (absolute ordinal values; then EF's unspecified batch insert order), fixed in f139e21/ad83cc4; the fixed test passes 6/6 on the final head. The five remaining full-suite failures from run (a) did not reproduce in run (b) and none touch the picker: the single run-(b) failure was the same picker assertion, now fixed. Honest limit: no third full-suite run was executed after that one-line test-only fix; the exact test passes on ad83cc46 and the authoritative full-suite evidence is the CI lane at Checkpoint C/D. The 51–57 minute wall time and the planner-local API-suite failures (`12-planner-full-local.log`, 44 failures under concurrent suites with an 8–9.5 GB testhost) are local-box resource degradation, not product regressions — the authoritative lanes run in GitHub Actions.

**Artifacts actually opened/inspected:** migration SQL and model snapshot (identity absence verified); psql `\d software_releases`, `pg_trigger`, `pg_sequences` output after `database update`; planner JSON (classification + step results); generator hash verification; EXPLAIN ANALYZE plan; every TRX named above; probe logs 08/10/13.**Prior finding IDs and dispositions:** A1040-A01/A02A/A02B/A02C/A03/A04 implemented per the Round-4 approved design, including the Round-4 instruction: Npgsql automatic identity strategy explicitly suppressed (`NpgsqlValueGenerationStrategy.None`); the generated column is a plain nullable bigint with no default — verified in the model snapshot and the applied schema.

**Remaining uncertainty, risks, or decisions:** (1) the local full-suite and planner-API runs on this Windows box took ~51 minutes and showed resource-degradation failures (aborted responses under an 8–9.5 GB testhost); the authoritative full-suite evidence is the CI lane, and the suite-result line below states the clean local rerun outcome; (2) browser journey, revoked-access continuation test, and larger-fixture latency numbers are deferred to C as noted; (3) `SAVE_BOUNDARY.md` required no change for this diff.

**Recommended verdict and why:** PASS for the core — the approved design is implemented exactly (including the Npgsql strategy instruction), migration safety is proven for empty install, preceding-schema upgrade with preserved legacy/controlled data and idempotent reapplication, both providers' guards behave as designed under deterministic concurrency schedules, the canonical race still passes through the unchanged barrier, and bounded in-database paging is evidenced — with C/D/E gates and remaining scope explicitly reserved.

Please independently return PASS, CHANGES REQUIRED, or NOT REVIEWABLE, identify the exact SHA/design and scope reviewed, list actionable findings, and state which next phase may proceed. I will wait for Sean to relay your actual response.

CHECKPOINT B — AWAITING ASTRA REVIEW. Checkpoints C/D/E remain; no Full-CI request, queue admission, rollout or issue-closure action has been taken.

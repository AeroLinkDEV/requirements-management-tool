import fs from 'node:fs'

const p = 'C:/Users/seanm/.zcode/cli/memories/projects/requirements-management-tool-45760c6e59abbb2e/memory/glm-1040-checkpoint-a.md'
let s = fs.readFileSync(p, 'utf8')
const lines = s.split('\n')
const v2 = "**IMPLEMENTATION STATUS (2026-09-24): PR #1098 v2 RETURNED (36-implementation-v2-packet.md, head 41b75ced653eb918bdffa805079cfcf8de4d8eb3) after Astra executed the candidate and reproduced six defects (I01-I06) — awaiting Astra. Fixes:** I01 gate verdict case EXITS on REFUSED/errors before any POST (integrated: stale/consumed/rerun/missing-self = 0 transport POSTs); I02 collect_pages(prefix, base-url) + ?/& separator + per-endpoint shape validation (events=bare array, run-lists=workflow_runs wrapper, product transcript validated — wrong shapes refuse); I03 check-product-run pin/poll MODES (pin: ATTEMPT <n> for running/success, completed failure NEVER pinned; poll: RUNNING/SUCCEEDED/FAILED + attempt-change refusal); I04 dispatch curl status captured — transport error/non-2xx/missing identity = UNCERTAIN → boundary-filtered discovery → bounded refusal, exactly 1 POST; I05 PENDING = newest UNFINISHED attempt (never a completed failure); I06 INTEGRATED harness: full run-block body extracted+executed against stateful python mock-curl (POST counts at HTTP boundary; NONE/EXHAUSTED eligible, all refusal families 0-POST, lost-response recovery+never-resolution, queued-successor, trust/attempt/404, success reuse, exact per-attempt jobs URL). Suite: 27 tests — 18 pass/9 skip(win32: integrated needs POSIX runtime→ubuntu gate)/0 fail; all 15 planner files 0 failures; YAML validated. Commits: eace33f6→efe0f53a (six fixes)→41b75ced (trace removal); PR #1098 body updated. DEBUG LESSONS (Windows): node -e nested quotes fragile → write .mjs fix scripts to files; template literals: bash-dollar-brace needs escaping — prefer array-join string building; JS template backslash-n vs real newline in generated python; heredoc extraction slice excludes closer; case pattern PENDING) never matches PENDING <id> (must be PENDING*); ::error:: echoes to STDOUT; rm files named --header needs rm -f --; mock -w semantics: format string printed to STDOUT. Remaining limits: return_run_details pinned-API-version qualification at acceptance; integrated scenarios execute in ubuntu gate (win32 blocked, documented); installation/rollback UNRESOLVED; PR #1098 not queued/labeled. #1066 stays draft 027c985e; Checkpoint D pending; D01–D03 closed."
lines[20] = v2
fs.writeFileSync(p, lines.join('\n'))
console.log('memory status replaced')

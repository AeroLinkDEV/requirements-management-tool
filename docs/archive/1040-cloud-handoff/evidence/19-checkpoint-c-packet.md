(Retained copy of the exact Checkpoint C packet submitted to Astra via Sean on 2026-09-21.)

ASTRA REVIEW REQUEST — #1040 — CHECKPOINT C — FINAL IMPLEMENTATION BEFORE FULL CI

**Requested decision and next action:**
Review the completed implementation on the exact head below (browser journey, access qualification, copied-host proof, larger-fixture evidence, final docs/planner/contract checks, draft PR). On PASS, the permitted next action is applying `ready-for-full-ci` to PR #1066 for that exact head and proceeding to Checkpoint D. No Full-CI request, queue admission, auto-merge, rollout or issue closure has been made.

**Exact head and status:** `6eb0a07c8b01a9112ffd7fb2f4fb2ee4f0da6850`, worktree clean, branch `glm/1040-release-picker-snapshot`, pushed to origin. Baseline `b64301b168ed98d4054cb893932978c4d090f745`; protected main `7404e884` (no rebase needed — no conflicts with #1064's files).

**Draft PR:** https://github.com/AeroLinkDEV/requirements-management-tool/pull/1066 — DRAFT, no `ready-for-full-ci` label, no auto-merge, non-closing `Refs #1040` (issue stays open). PR title (squash-commit title): "Freeze Release relationship-picker candidates across cursor pages".

**Problem and final behavior:** unchanged from the approved design. The build picker freezes candidate membership at page one (database-owned, project-fenced insertion ordinal; NULL = documented legacy cohort), continuations replay the frozen cutoff, fresh traversals re-fence, old tokens fail closed, authorization stays live, and the saved relationship carries the exact selected Release identity and canonical deep link. Canonical ordering, controlled identities and all non-Release paging are untouched.

**Changed files (all, over baseline):** ProgramRecord.cs; AeroLinkDbContext.cs; migration 20260921140636 (+Designer, +snapshot); ReleasePickerSqliteGuard.cs (new); Program.cs; ManagedDocumentEndpoints.cs; ManagedDocumentPaging.cs; docs MANAGED_DOCUMENTATION_CENTER.md; generated api-test-intent.json / api-host-classification.json / route-coverage.json / API_TEST_INTENT_INVENTORY.md; three new/extended test files (ProjectSetupPostgresQualificationTests.ReleasePickerSnapshot.cs, ReleasePickerMembershipApiTests.cs, ReleasePickerSqliteGuardTests.cs) and the new browser journey (managed-documentation-center-picker-continuation.spec.ts). No scope change since B: product source gained no new mechanism (only the Checkpoint-C access/scale/browser tests and the PR/CI bookkeeping).

**Browser journey (R09):** `product/client/tests/managed-documentation-center-picker-continuation.spec.ts`, Playwright/Chromium against the disposable SQLite API + Vite dev server (the repository's standard browser harness; two green runs: 2.5 m and 3.1 m). It drives the REAL Documentation Center UI: register → SDP document → links tab → "+ Link artifact" → Artifact type "Build" over a 57-candidate fixture (55 seeded directly into the run's disposable SQLite database — through the shipped allocator trigger — plus the two showcase builds; seeding documented as a fixture, including the uppercase Guid text representation Microsoft.Data.Sqlite persists). Assertions: page one = placeholder + 50; a build committed after page one is **absent from the "Load more records" continuation** (frozen membership, count 58 without it); a build that exists only on page two is selectable; saving links the document revision; the Links tab shows the relationship row with href `/releases/{selectedId}/command-center` for the **exact selected Release id**; inserting one more build and re-fencing via a fresh dialog shows it (count 60). Route mocks are not used — the journey runs real HTTP against the API host.

**Access qualification (R06 — now complete):** two new API facts. `Revoked_project_access_denies_the_frozen_continuation_and_relationship_write`: an Engineer member reads a frozen traversal, the membership is revoked, and both the frozen continuation and the relationship write return 403 with no candidate disclosure. `Foreign_project_picker_reads_and_relationship_writes_are_refused`: a member of one Program reading another Program's picker is refused 403, and a relationship write carrying another Project's Release id is refused (403/400 fail-closed before mutation).

**Copied-host proof:** `The_api_host_installs_the_guards_before_serving_and_a_copied_database_restarts_cleanly` asserts a live API host has all three triggers and the cohort flag on its served database, and that a **copied** guarded database file (WAL-checkpointed before copy) restarts without reclassification, keeps the legacy flag, and allocates new membership exactly once. The installer itself is additionally covered by first-install, failed-install atomic rollback and restart tests.

**Larger-fixture evidence (R08):** new required-runner fact `Picker_continuation_on_a_larger_fixture_stays_bounded_and_records_latency` — 601 builds (inserted through the shipped trigger), page one 100 rows (107 ms), continuation 100 rows (38 ms), full traversal of 601 candidates with zero duplicates, captured continuation command containing the project scope, membership cutoff, after-key predicate and `LIMIT 101`, EXPLAIN (ANALYZE): Limit → Sort (101 rows materialized) → Index Scan `IX_software_releases_ProjectId_CanonicalIdentity` with the membership+keyset predicates evaluated in-database. Honest cost statement: the membership predicate is a filter over the project-scoped index (not index-only); numbers are from the local disposable container and larger fixtures stay C-scope as agreed.

**Documentation/contracts:** MANAGED_DOCUMENTATION_CENTER.md Release section replaced with the implemented guarantee (legacy cohort, frozen continuations, old-token fail-closed, provider notes); no SAVE_BOUNDARY change (diff confirmed); no DEC/PROJECT_STATE change required. Generated contracts regenerated — two consecutive runs byte-identical; `Test-RepositoryLayout.ps1` passed; changed-area planner (Fast, final head) classification selected backend/client/browser/postgreSQL and completed passed.

**Required PostgreSQL qualification (exact commands/results, tied to this content):** `Test-ProjectSetupPostgres.ps1` → **Infrastructure 11/11 passed, API 11/11 passed, none skipped** (`trx-required-pg-c/`); TRX TRX file `…Api.Tests-ffac2f58…trx` names all eleven Api results, all Passed: Picker_upgrade_preserves_legacy_rows_and_freezes_continuations; Picker_database_guard_rejects_forbidden_membership_mutations; Picker_continuation_generated_sql_and_parameters_are_captured_for_plan_evidence; Picker_continuation_on_a_larger_fixture_stays_bounded_and_records_latency; Stale_snapshot_writers_cannot_enter_frozen_release_continuations (both isolation levels); Page_one_fence_waits_for_uncommitted_allocator_then_includes_the_committed_build; Cancelled_page_one_releases_the_fence_without_stuck_writers; Fresh_project_services…; Release_creation_and_legacy_import…; Discard_cannot_win…; Repository_edit_waits…. Provenance precision: the gate executed from the working tree whose C#/product content is identical to head 6eb0a07 (the head adds only the client spec and generated artifacts); SQLite focused set 14/14 on the same content.

**Full-suite status (unchanged, plainly stated):** no clean exact-final-head full local API-suite rerun exists; the retained history (1123/6 → 1128/1 with the single failure fixed and passing since), the planner-local contention failures and the resource-degradation hypothesis are documented; the authoritative broad-suite evidence is the GitHub Actions lane this PR will run at Checkpoint D.

**R01–R12:** R01 done (A); R02, R03, R04 (now incl. manifest payload/hash and ElectronicSignature preservation), R05, R06 (revoked + foreign now included), R07, R08, R09 (PG none-skipped + SQLite separate + real browser journey), R10 — complete. R11 (exact-head Full Product readiness, integration-readiness review, protected composed-candidate checks, HOME rollout observation, supported upgrade path) and R12 (post-deployment read acceptance) remain for D/E — not started.

**Known limitations:** SQLite guard coverage is installer-level plus trigger-level (the database-level guard claim remains PostgreSQL-only); browser seeding writes directly to the disposable run database (uppercase Guid text documented); local full-suite wall time is degraded (~51–57 min) with a retained unconfirmed resource-contention hypothesis; larger-fixture latency was measured on the local container only.

**Prior finding dispositions:** all A-side and B-side findings (A1040-A01–A04; B1040-B01–B06) closed and re-verified in the retained runs; no new scope was added beyond the C items Astra authorized.

**Recommended verdict and why:** PASS for Checkpoint C — the implementation is complete and faithful to the approved design on the exact head, every acceptance criterion of B/C scope now has executed evidence (browser journey included), access qualification is closed, documentation and generated contracts are current and stable, and the PR is open as a draft awaiting the exact-head Full CI authorization that only this review can grant.

Please independently return PASS, CHANGES REQUIRED, or NOT REVIEWABLE, identify the exact SHA/design and scope reviewed, list actionable findings, and state which next phase may proceed. I will wait for Sean to relay your actual response.

CHECKPOINT C — AWAITING ASTRA REVIEW. D/E gates remain; no Full-CI request, queue admission, rollout or issue-closure action has been taken.

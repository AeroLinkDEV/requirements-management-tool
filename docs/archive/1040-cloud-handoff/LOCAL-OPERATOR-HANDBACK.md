# Cloud/local boundary and hand-back contract

Cloud Claude can complete source investigation, scoped implementation, disposable-provider tests, evidence mapping, GitHub review preparation and otherwise-authorized protected integration. It cannot claim access to Sean's Windows machine, Task Scheduler, local Git configuration or HOME database.

## Windows checkout recovery packet

Provide Astra/Sean one bounded proposal containing:

1. The exact local repo path and observed incident identities from ASTRA-CORRECTIONS.md, marked for local refresh.
2. A read-only audit of local HEAD/refs/reflog/status/configuration origins and worktree ownership, plus identification of any work since the incident.
3. Preservation of accidental commits and legitimate work, including verification of the bundle prerequisite. Preserve logs/configuration separately; the Git bundle does not capture them.
4. Exact proposed target refs/tree/configuration, commands and their preconditions; an explicit distinction between undoing the incident and updating an older local main to today's remote main.
5. Verification that unrelated #1105 content and all legitimate work survive, with a reversible recovery path.
6. An actual Astra review before local ref/config changes. Do not assume branch deletion is required or guess previous author settings.

Do not block independent cloud review and delivery planning while awaiting this local action. Never push the accidentally modified local main or import its tip into a feature candidate.

## HOME rollout and final acceptance packet

Before integration readiness, prepare the supported current operator procedure from protected-main contracts. Name the expected merge/deployment identity, migration `20260921140636_AddReleasePickerMembership`, backup/isolated-copy qualification requirements, expected receipts/health checks and recovery behavior.

After authorized merge/deployment, request attributable readback from the local operator: exact source/deployed identity, relation to the feature merge, migration state through the supported path, healthy serving state and focused read-only picker behavior on existing records. Any current-main advancement must be accounted for explicitly.

Keep destructive/insert/concurrency tests on disposable state. Persistent PostgreSQL port 54329 and `product/.local` are not test fixtures. Never manually rewrite controlled data, signatures, manifests or migration history. Do not expose HOME credentials to the cloud as a shortcut.

## Process ownership

GLM reported Vite PID 27364 on localhost port 5199; that is historical, not current process identity. A local operator must verify current PID creation identity, command line, ownership and port before stopping anything. Containers `rmt1040-impl-pg` and `aerolink-1023-qualification` were reported as predating the handoff and belonging to other work; do not stop or repurpose them based on this document.

## Final closure boundary

Cloud CI success is not HOME acceptance. Keep #1040 open until R01–R12 and actual Astra post-deployment acceptance are complete. Local-only limitations should produce a precise hand-back, not fabricated success or a claim that the entire cloud task is impossible.

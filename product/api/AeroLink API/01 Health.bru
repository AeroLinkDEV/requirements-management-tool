meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

tests {
  test("API is healthy", function() {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("healthy");
  });
}

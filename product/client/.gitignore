# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
playwright-report/
test-results/
playwright-report-*/
test-results-*/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
api-logs/

# Per-run browser evidence. `AEROLINK_E2E_OUTPUT_DIR` points a run at its own directory so one run
# cannot clear another run's captures; selected images are promoted to product/docs/screenshots
# deliberately, and the run's own copies stay out of the commit.
run-output/
